# Chima Coffee - Example Environment Variables
# Copy this file to `.env.local` for local development
# Add the same variables in Vercel Project Settings → Environment Variables
# Note: Only variables prefixed with NEXT_PUBLIC_ are exposed to the browser

# Public (browser) variables
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# Server-only secrets (never expose to the browser)
STRIPE_SECRET_KEY=
# Optional: Only if you use server-side Supabase admin features (not required in this project)
# SUPABASE_SERVICE_ROLE_KEY=

# Admin UI/session security
# Long random base64url or ascii secret; rotate quarterly. On rotation, move previous secret(s) to ADMIN_SESSION_SECRET_PREVIOUS
ADMIN_SESSION_SECRET=
# Comma-separated list of previous secrets to support seamless rotation
ADMIN_SESSION_SECRET_PREVIOUS=
# Admin login password (if not fronted by identity provider)
ADMIN_UI_PASSWORD=
# Session TTL in seconds (default 604800 = 7 days)
ADMIN_SESSION_TTL_SEC=604800
# Failed-attempts window and threshold
ADMIN_FAILURE_WINDOW_SEC=900
ADMIN_LOCKOUT_THRESHOLD=3
# Optional explicit expiry of failure records
ADMIN_FAILURE_EXPIRE_SEC=86400
# Optional denylist of revoked session JTIs (comma-separated)
ADMIN_JTI_DENYLIST=
# Canonical host (enforces a 301 to prevent cookie split). Example: www.chimacoffee.com
CANONICAL_HOST=

# hCaptcha (used for progressive challenge on admin login)
HCAPTCHA_SITEKEY=
HCAPTCHA_SECRET=

# Analytics (optional)
GA_MEASUREMENT_ID=

# Optional: Custom site URL if you need to reference it explicitly
# NEXT_PUBLIC_SITE_URL=
